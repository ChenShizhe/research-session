---
label: stub::detection-placeholder
kind: stub
status: stub
title: Detection-signature stub
manuscript-anchor: TBD
last-synced: {{SCAFFOLD_DATE}}
---

# Detection-signature stub

This file exists so the research-meeting skill's theory-graph detection
signature passes (verifier present + at least one populated theoretical-object
subdirectory). It carries no theoretical content.

The stub is removed automatically by `theory-vault-writer` `add-object` after
the first non-stub node lands in this vault. Do not author content into this
file. Use `theory-vault-writer` `add-object` to create the first real node.

The stub's `kind: stub` and `status: stub` frontmatter exempt it from
manuscript-coupled checks (V9–V14) even when `_meta/severity-overrides.yaml`
is removed.
