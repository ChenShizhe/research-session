interface:
  display_name: "Voice Narrator"
  short_description: "Produce segmented narrated slide videos"
  default_prompt: "Use $voice-narrator to turn stable slide images and per-slide narration into a local narrated video."
policy:
  allow_implicit_invocation: true
